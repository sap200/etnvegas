<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/gradient_background"
    tools:context=".RouletteWheelSpinActivity">

    <FrameLayout
        android:id="@+id/my_frame"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="10dp"
        android:foregroundGravity="center"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/rouletteWheel"
            android:layout_width="381dp"
            android:layout_height="342dp"
            android:layout_gravity="center"
            android:layout_marginStart="5dp"
            android:layout_marginTop="4dp"
            android:layout_marginEnd="5dp"
            android:scaleType="fitCenter"
            app:srcCompat="@drawable/my_r_wheel_d" />

        <ImageView
            android:id="@+id/ball"
            android:layout_width="15dp"
            android:layout_height="15dp"
            app:srcCompat="@drawable/woman"
            android:layout_gravity="center"
            tools:layout_editor_absoluteX="252dp"
            tools:layout_editor_absoluteY="239dp" />
    </FrameLayout>

    <TextView
        android:id="@+id/roulette_choice_number"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginStart="10dp"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="10dp"
        android:background="@drawable/edit_text_box"
        android:ems="10"
        android:fontFamily="@font/marko_one"
        android:gravity="center"
        android:text="Note: Please do not quit the screen while spinning"
        android:textColor="#E40E0E"
        android:textStyle="bold"
        android:visibility="visible"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/my_frame" />

</androidx.constraintlayout.widget.ConstraintLayout>